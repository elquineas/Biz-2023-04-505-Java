package com.callor.classes.arrays;

public class ArrayB {
	public static void main(String[] args) {
		int[] nums = new int[10];
		System.out.println(nums[0]);
		
		nums[0] = 100;
		
		/*
		 * 배열에 값을 저장하기
		 * 배열[index] = 값
		 * 
		 * 배열에서 값을 읽을때 
		 * result = 배열[index]
		 * 
		 * index : 첨자 라고한다
		 * index는 항상 0부터 시작한다
		 * index의 끝값은 배욜의 개수-1 이다
		 * 
		 */
		
//		nums[10] = 100;
	}
}
